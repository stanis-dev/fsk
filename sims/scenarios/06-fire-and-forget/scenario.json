{
  "id": "06-fire-and-forget",
  "title": "Fire-and-forget (no polling)",
  "tier": 3,
  "capability": "poll the record to FINISHED before treating a receipt as issued",
  "persona_ref": "PERSONA.md Tier 3 silent catastrophe (fire-and-forget never reaches the authority); NOTES.md step 11 (LIVE is async, poll to FINISHED)",
  "traps": [
    { "kind": "silent-bug", "where": "fixture/fiskaly.go (issueReceipt)", "detail": "returns success on the accepted TRANSACTION (PROCESSING) and never polls to FINISHED; looks fine in TEST, silently fails on LIVE", "correct": "poll the record (GET /records/{id}) until state/mode FINISHED, fail the completion if it does not reach FINISHED" }
  ],
  "judge": { "rules": ["fiskaly-host", "token-exchange", "idempotency-key", "api-version", "records-flow", "polling"] }
}
