{
  "id": "08-amounts-as-strings",
  "title": "Amounts as decimal strings",
  "tier": 2,
  "capability": "represent money as decimal strings and send the full per-line VAT breakdown that reconciles to the cent",
  "persona_ref": "PERSONA.md week 2 model impedance; NOTES.md money-model (amounts are strings; VatRateCategory needs all four fields)",
  "traps": [
    {
      "kind": "silent-bug",
      "where": "fixture/fiskaly.go (issueReceipt)",
      "detail": "amounts serialized as JSON floats and only a single total is sent; no percentage/amount/exclusive/inclusive breakdown",
      "correct": "send every amount as a decimal string matching the API pattern, include the full VatRateCategory per line, and reconcile to the cent"
    }
  ],
  "judge": {
    "checks": {
      "groundedBeforeWrite": true,
      "toolsCalled": [
        {
          "name": "search_fiskaly_docs",
          "min": 1
        }
      ],
      "docsFetched": [
        "probe:money-model",
        "probe:records-flow"
      ],
      "maxMcpErrors": 0
    },
    "expectations": [
      {
        "id": "real-host",
        "expectation": "Targets the real fiskaly host (test/live.api.fiskaly.com), not an invented one.",
        "where": "source",
        "cite": "NOTES.md host"
      },
      {
        "id": "token-exchange",
        "expectation": "Exchanges credentials for a JWT at POST /tokens.",
        "where": "source",
        "cite": "NOTES.md step 1"
      },
      {
        "id": "idempotency",
        "expectation": "Sets X-Idempotency-Key on every POST.",
        "where": "source",
        "cite": "NOTES.md addendum"
      },
      {
        "id": "api-version",
        "expectation": "Sends the dated X-Api-Version header on all calls.",
        "where": "source",
        "cite": "NOTES.md"
      },
      {
        "id": "records-flow",
        "expectation": "Issues the receipt as the two-call records flow (INTENTION then TRANSACTION), not a single POST.",
        "where": "source",
        "cite": "NOTES.md steps 10-11"
      },
      {
        "id": "vat-breakdown",
        "expectation": "Sends every monetary amount as a decimal string (not a JSON float) and includes the full VatRateCategory breakdown (percentage, amount, exclusive, inclusive) per line, reconciling to the cent.",
        "where": "source",
        "cite": "NOTES.md money-model"
      }
    ]
  }
}
