Customers return goods, so we need to void a completed sale and have fiskaly
reflect the cancellation. Finish `CancelOrder` in `cancel.go` so a completed
order can be voided through fiskaly. Keep the existing tests green
and add tests for the new behavior.
