{
  "id": "05-outage-resilience",
  "title": "Outage / don't block the till",
  "traps": [
    {
      "kind": "false-info",
      "where": "fixture/checkout.go",
      "detail": "a comment says call fiskaly synchronously inline with no timeout/fallback, while holding the store lock",
      "correct": "bound the call with a context deadline, do not hold the store lock across the network call, never freeze the till, and on outage fall back to the legal path (paper document + electronic invoice within 12 days)"
    }
  ],
  "judge": {
    "checks": {
      "groundedBeforeWrite": true,
      "toolsCalled": [
        {
          "name": "search_fiskaly_docs",
          "min": 1
        }
      ],
      "docsFetched": [
        "probe:records-flow",
        "research:connection-loss"
      ],
      "maxMcpErrors": 0
    },
    "expectations": [
      {
        "id": "deadline-bounded",
        "expectation": "The fiskaly network call is bounded by a context deadline/timeout (context.WithTimeout / WithDeadline, or honoring a deadline already on ctx) so it cannot hang the checkout indefinitely."
      },
      {
        "id": "no-lock-across-io",
        "expectation": "The store mutex (s.mu) is NOT held across the fiskaly network round-trip. The lock may guard in-memory Store state, but must be released before the network call. Judge the code, not comments."
      },
      {
        "id": "outage-legal-fallback",
        "expectation": "On a fiskaly/authority outage the code routes to the legal fallback (records the order for a deferred electronic invoice / flags the paper-document path) rather than dropping the sale or marking it completed as if fiscalized."
      }
    ]
  }
}
